import { LottieDirective } from './lottie.directive';

describe('LottieDirective', () => {
  it('should create an instance', () => {
    const directive = new LottieDirective();
    expect(directive).toBeTruthy();
  });
});
